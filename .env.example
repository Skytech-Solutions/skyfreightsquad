# Copy to .env.local and fill in values. Vars must be prefixed VITE_ to be exposed to the client.
# No env vars are required at present. Add entries here as you introduce them.

# Example:
# VITE_PUBLIC_SITE_URL=https://skyfreightsquad.com
